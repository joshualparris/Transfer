import type{DashboardData}from'../../../electron/types';
type Act=(label:string,fn:()=>Promise<any>)=>Promise<any>;const n=(v:unknown)=>Number(v??0).toLocaleString();
export default function CalendarPage({data,busy,act}:{data:DashboardData;busy:boolean;act:Act}){const source=data.accounts.find(a=>a.role==='source'),dest=data.accounts.find(a=>a.role==='destination'),granted=dest?.scopes.includes('https://www.googleapis.com/auth/calendar.app.created'),s=data.calendar.stats,p=data.calendar.progress;return <><section className="panel"><div className="paneltitle"><div><p className="eyebrow">PHASE 4 · CALENDAR</p><h2>{source?.email??'Source'} → {dest?.email??'Destination'}</h2></div><span>{data.calendar.running?'Running':'Resumable'}</span></div><p>Source access is read-only. Lifeboat creates separately named destination calendars, preserves recurring-event rules, and never deletes or edits source events.</p><button disabled={busy||granted} onClick={()=>act('calendar-auth',()=>window.lifeboat.authorizeCalendar())}>{granted?'Destination Calendar authorised':'Authorise destination Calendar'}</button></section><div className="metrics"><div className="metric"><small>Discovered</small><b>{n(s.discovered)}</b></div><div className="metric"><small>Copied</small><b>{n(s.copied)}</b></div><div className="metric"><small>Verified</small><b>{n(s.verified)}</b></div><div className="metric"><small>Recurring</small><b>{n(s.recurring)}</b></div></div><section className="panel"><h3>Copy and independent backup</h3><div className="actions"><button disabled={busy||data.calendar.running} onClick={()=>act('calendar-discover',()=>window.lifeboat.discoverCalendar())}>Dry-run Calendar inventory</button><button disabled={busy||data.calendar.running||!granted||!s.discovered} onClick={()=>act('calendar-start',()=>window.lifeboat.startCalendar())}>Start confirmed Calendar migration</button><button className="secondary" disabled={busy} onClick={()=>act('calendar-export',()=>window.lifeboat.exportCalendars())}>Export ICS backups</button></div><p>{p.operation??'Idle'} {p.copied?`· ${n(p.copied)} copied`:''}</p></section><section className="panel"><h3>Destination-only verification</h3><p>Checks the stored event pairing, summary fingerprint, dates, times and recurrence rules using only destination access.</p><button disabled={busy||!s.copied} onClick={()=>act('calendar-verify',()=>window.lifeboat.verifyCalendarDestinationOnly())}>Verify destination calendars</button></section></>}
