import { writeFile, mkdir } from 'node:fs/promises';
import path from 'node:path';
import type { InventorySnapshot } from './types';
const esc=(v:unknown)=>`"${String(v??'').replaceAll('"','""')}"`;
export async function exportReports(dir:string,snapshot:InventorySnapshot,queue:unknown[]){await mkdir(dir,{recursive:true});const stamp=snapshot.createdAt.replaceAll(':','-');const base=`lifeboat-inventory-${stamp}`;const payload={generatedAt:new Date().toISOString(),snapshot,queue};await writeFile(path.join(dir,`${base}.json`),JSON.stringify(payload,null,2));const rows=['module,metric,value',`gmail,messages,${snapshot.gmail.messages}`,`gmail,threads,${snapshot.gmail.threads}`,`drive,files,${snapshot.drive.files}`,`drive,bytes,${snapshot.drive.bytes}`,`contacts,contacts,${snapshot.contacts.contacts}`,`calendar,events,${snapshot.calendar.events}`,...snapshot.gmail.labels.map(x=>['gmail-label',esc(x.name),x.messages].join(','))];await writeFile(path.join(dir,`${base}.csv`),rows.join('\n'));const h=(s:unknown)=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]!));const html=`<!doctype html><meta charset="utf-8"><title>Cornerstone Lifeboat inventory</title><style>body{font:16px system-ui;max-width:900px;margin:40px auto;color:#17251d}h1{color:#155b3a}.card{padding:18px;border:1px solid #cad8cf;border-radius:12px;margin:12px 0}.warn{color:#9b4216}</style><h1>Cornerstone Lifeboat inventory</h1><p>Account: ${h(snapshot.account)} · ${h(snapshot.createdAt)}</p>${snapshot.errors.length?`<div class="card warn"><b>Incomplete inventory</b><ul>${snapshot.errors.map(e=>`<li>${h(e.module)}: ${h(e.message)}</li>`).join('')}</ul></div>`:''}<div class="card"><h2>Gmail</h2><p>${snapshot.gmail.messages.toLocaleString()} messages · ${snapshot.gmail.threads.toLocaleString()} threads · ${snapshot.gmail.labels.length} labels</p></div><div class="card"><h2>Drive</h2><p>${snapshot.drive.files.toLocaleString()} files · ${snapshot.drive.folders.toLocaleString()} folders · ${snapshot.drive.googleNative.toLocaleString()} Google-native files · ${snapshot.drive.bytes.toLocaleString()} bytes enumerated</p></div><div class="card"><h2>Contacts</h2><p>${snapshot.contacts.contacts.toLocaleString()} contacts · ${snapshot.contacts.groups.toLocaleString()} groups · ${snapshot.contacts.otherContacts??'Unavailable'} other contacts</p></div><div class="card"><h2>Calendar</h2><p>${snapshot.calendar.calendars.toLocaleString()} calendars · ${snapshot.calendar.events.toLocaleString()} events · ${snapshot.calendar.recurring.toLocaleString()} recurring series</p></div><p><b>This inventory is not a migration-complete or safe-to-lose-source report.</b></p>`;await writeFile(path.join(dir,`${base}.html`),html);return dir;}
